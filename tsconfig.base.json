{
  "comment": "Generated by `pnpm eject`. The monorepo tsconfig.base.json holds @gauntlet/* and @app-brew/* path aliases into libs/ source; in a standalone repo those resolve from node_modules (verdaccio packages, via the exports field), so paths is intentionally empty. @app-brew/build-tools (rspack/babel) require()s this file from the repo root.",
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {}
  }
}
